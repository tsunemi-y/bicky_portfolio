interface Colors {
  primary: string;
  secondary: string;
  mainText: string;
  subText: string;
  dangerText: string;
}

export const colors: Colors = {
  primary: "#fdffe5",
  secondary: "#72cc82",
  mainText: "var(--muidocs-palette-grey-900, #1A2027)",
  subText: "#ffffff",
  dangerText: "#dc3545",
};
